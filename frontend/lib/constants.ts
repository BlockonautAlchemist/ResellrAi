/**
 * Frontend Constants
 *
 * Shared constants used across the application.
 */

// Device identity is now handled by frontend/lib/identity.ts
// TEMP_USER_ID has been removed — each device gets a unique anonymous UUID.
